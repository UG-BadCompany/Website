(async()=>{if(!await TACompany.requireInstalled())return;await TACompany.load();document.getElementById('login-form').addEventListener('submit',async e=>{e.preventDefault();const s=document.getElementById('login-status');s.textContent='Sending secure link...';try{await TAAuth.login(new FormData(e.currentTarget).get('email'));s.textContent='Check your email for your secure sign-in link.'}catch(err){s.textContent=err.message||'Unable to send link.'}});})();
