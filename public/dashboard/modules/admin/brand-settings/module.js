window.TAModules.register({id:'admin.brand-settings',role:'admin',title:'Brand Settings',icon:'🎨',permissions:['branding.manage'],async mount({root,api,company}){const c={...company};const colors=['primaryColor','accentColor','backgroundColor','surfaceColor','textColor','buttonColor','successColor','warningColor','dangerColor'];const labels={primaryColor:'Primary Color',accentColor:'Accent Color',backgroundColor:'Background Color',surfaceColor:'Surface/Card Color',textColor:'Text Color',buttonColor:'Button Color',successColor:'Success Color',warningColor:'Warning Color',dangerColor:'Danger Color'};root.innerHTML=`<section class="card stack"><h2>Brand Settings</h2><p>Update white-label company branding and global CSS-variable theme. Logo is URL-based until upload storage is connected.</p><div class="grid grid-2"><div class="field"><label>Company Name</label><input name="companyName" value="${c.companyName||''}"></div><div class="field"><label>Display Name</label><input name="displayName" value="${c.displayName||''}"></div><div class="field"><label>Logo URL</label><input name="logoUrl" value="${c.logoUrl||''}"></div><div class="field"><label>Favicon URL</label><input name="faviconUrl" value="${c.faviconUrl||''}"></div><div class="field"><label>Theme</label><select name="themeMode"><option value="system">System</option><option value="dark">Dark</option><option value="light">Light</option></select></div><label class="pill"><input type="checkbox" name="enableThemeToggle" ${c.enableThemeToggle!==false?'checked':''}> Enable Theme Toggle</label><label class="pill"><input type="checkbox" name="showCompanyNameInHeader" ${c.showCompanyNameInHeader?'checked':''}> Show Company Name Beside Logo</label></div><h3>Colors</h3><div class="grid grid-3">${colors.map(n=>`<div class="field"><label>${labels[n]}</label><input type="color" name="${n}" value="${c[n]||TATheme.defaults[n]}"></div>`).join('')}</div><p id="brand-status"></p><p><button class="btn" id="save-brand">Save Brand Settings</button></p></section>`;root.querySelector('[name="themeMode"]').value=c.themeMode||'system';root.querySelectorAll('input[type="color"],select').forEach(el=>el.addEventListener('input',()=>{const next={...c,...TAForms.values(root)};next.showCompanyNameInHeader=root.querySelector('[name="showCompanyNameInHeader"]').checked;TATheme.apply(next)}));document.getElementById('save-brand').onclick=async()=>{const payload=TAForms.values(root);payload.showCompanyNameInHeader=root.querySelector('[name="showCompanyNameInHeader"]').checked;payload.enableThemeToggle=root.querySelector('[name="enableThemeToggle"]').checked;document.getElementById('brand-status').textContent='Saving...';const saved=await api.patch('/.netlify/functions/company-settings',payload);window.TACompany.apply(saved.company);document.getElementById('brand-status').textContent='Brand settings saved.'}},async destroy(){},async refresh(){}});
