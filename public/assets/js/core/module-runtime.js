window.TAModules = window.TAModules || { registry:new Map(), register(mod){ if(!mod?.id||typeof mod.mount!=='function') throw new Error('Invalid module registration'); this.registry.set(mod.id,mod); }, get(id){ return this.registry.get(id); } };
export async function mountModule(module, context){ const root=context.root; root.innerHTML='<div class="card">Loading module…</div>'; const html=await fetch(`${module.frontend.basePath}/${module.frontend.html}`).then(r=>r.text()); root.innerHTML=html; if(module.frontend.css&&!document.querySelector(`link[data-module-css="${module.id}"]`)){ const link=document.createElement('link'); link.rel='stylesheet'; link.href=`${module.frontend.basePath}/${module.frontend.css}`; link.dataset.moduleCss=module.id; document.head.appendChild(link); } await import(`${module.frontend.basePath}/${module.frontend.entry}`); const registered=window.TAModules.get(module.frontend.registerId||module.id); if(!registered) throw new Error(`Module ${module.id} did not register`); return registered.mount({...context,module}); }
