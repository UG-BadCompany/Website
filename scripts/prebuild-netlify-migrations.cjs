const fs=require('fs'); const path=require('path'); const dir='netlify/database/migrations'; fs.mkdirSync(dir,{recursive:true}); const files=fs.readdirSync(dir).filter(f=>f.endsWith('.sql')).sort(); const nums=new Set(); for(const f of files){const n=f.match(/^(\d+)/)?.[1]; if(!n) throw new Error(`Migration lacks numeric prefix: ${f}`); if(nums.has(n)) throw new Error(`Duplicate migration number ${n}`); nums.add(n);} console.log(`Validated ${files.length} migrations.`);
