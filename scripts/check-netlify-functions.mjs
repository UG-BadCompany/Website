import { existsSync, readdirSync } from 'node:fs'; const required=['install-status.mjs','install.mjs','install-env-status.mjs','install-env.mjs','dashboard-bootstrap.mjs','module-api.mjs','api.mjs']; const missing=required.filter(f=>!existsSync(`netlify/functions/${f}`)); if(missing.length){console.error('Missing functions: '+missing.join(', '));process.exit(1)} console.log(`Validated ${readdirSync('netlify/functions').filter(f=>f.endsWith('.mjs')).length} Netlify functions.`);
