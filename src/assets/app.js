const INSTALL_KEY = 'bc_platform_installation_complete';
const DRAFT_KEY = 'bc_platform_install_draft';
const WORKFLOW_KEY = 'bc_platform_workflows';
const app = document.getElementById('app');
const modules = [];
const moduleNames = ['Request Intake','Quote Center','Work Orders','Scheduling','Inventory','Invoices','Square Payments','Finance','AI Photo Estimate','AI Quote','AI Troubleshooting','File Manager','Theme Manager','Homepage Editor','Module Manager','Platform Health','Cache Manager','Audit Logs','Backup & Restore','Workflow Engine','Client Portal','Users & Permissions','Environment & Integrations'];
const moduleRoutes = ['/request-estimate/','/dashboard/quotes','/dashboard/work-orders','/dashboard/scheduling','/dashboard/inventory','/dashboard/invoices','/dashboard/payments','/dashboard/finance','/dashboard/ai/photo-estimate','/dashboard/ai/quote','/dashboard/ai/troubleshooting','/dashboard/system/files','/dashboard/system/theme','/dashboard/system/homepage','/dashboard/system/modules','/dashboard/system/health','/dashboard/system/cache','/dashboard/system/audit','/dashboard/system/backup','/dashboard/workflow','/portal/','/dashboard/system/users','/dashboard/system/integrations'];
moduleNames.forEach((name,i)=>modules.push({id:name.toLowerCase().replaceAll(' & ','-').replaceAll(' ','-'),name,route:moduleRoutes[i],category: i<1?'public':i<8?'operations':i<11?'ai':'system'}));

function installed(){return localStorage.getItem(INSTALL_KEY)==='true'}
function setInstalled(v){localStorage.setItem(INSTALL_KEY, v?'true':'false')}
function draft(){return JSON.parse(localStorage.getItem(DRAFT_KEY)||'{}')}
function saveDraft(data){localStorage.setItem(DRAFT_KEY, JSON.stringify({...draft(),...data}))}
function navigate(path){history.pushState({},'',path); render()}
async function installStatus(){try{const r=await fetch('/api/install-status'); return await r.json()}catch{return {ok:true,installed:installed(),installationComplete:installed(),needsInstall:!installed(),currentStep:'welcome'}}}
function applyTheme(mode='system'){document.body.classList.toggle('dark', mode==='dark' || (mode==='system' && matchMedia('(prefers-color-scheme: dark)').matches))}
function shell(content){return `<header class="topbar"><div class="shell nav"><div class="brand"><div class="logo"></div><span>Contractor CMMS + AI Quoting</span></div><nav class="links"><a href="/" data-link>Home</a><a href="/request-estimate/" data-link>Request Estimate</a><a href="/portal/" data-link>Client Portal</a><a class="primary" href="/dashboard/" data-link>Dashboard</a></nav></div></header><main class="shell">${content}</main>`}
function bindLinks(){document.querySelectorAll('[data-link]').forEach(a=>a.addEventListener('click',e=>{e.preventDefault();navigate(a.getAttribute('href'))}));}
function installWizard(recovery=false){const d=draft(); const required=['companyProfile','ownerAccount','themeSettings','homepageConfig','moduleRegistry','servicesTrades']; const complete=Object.fromEntries(required.map(k=>[k,!!d[k]])); const steps=['Welcome','Company','Theme','Owner','Modules','Services','Homepage','Review'];
app.innerHTML=shell(`<section class="card"><span class="badge warning">Installer first</span><h1>${recovery?'Installation Recovery':'First-run Installer'}</h1><p>The platform is locked until setup finishes. Optional OpenAI, Resend, Square, SMTP, SerpAPI, and license integrations are warnings only and are configured later in System Center → Environment & Integrations.</p><div class="stepper">${steps.map((s,i)=>`<span class="step ${i<7?'done':''}">${i+1}</span>`).join('')}</div>${recovery?`<div class="banner">Recovery shows completed and incomplete setup steps. No business pages are exposed until Finish Install succeeds.</div>`:''}</section><section class="grid two"><div class="card"><h2>Required core setup</h2>${required.map(k=>`<p><span class="badge ${complete[k]?'ok':'warning'}">${complete[k]?'complete':'needed'}</span> ${label(k)}</p>`).join('')}<button class="btn" id="quickfill">Use polished starter configuration</button></div><div class="card"><h2>Optional integrations</h2>${['OpenAI AI services','Resend email','Square payments','SMTP','SerpAPI','License server'].map(x=>`<p><span class="badge warning">warning only</span> ${x}</p>`).join('')}<p>Missing integrations never block installation.</p></div></section><section class="card"><h2>Setup form</h2><div class="form"><input id="company" placeholder="Company name" value="${d.companyName||''}"><input id="owner" placeholder="Owner email" value="${d.ownerEmail||''}"><select id="theme"><option>system</option><option>light</option><option>dark</option><option>custom</option></select><textarea id="services" placeholder="Services/trades, comma separated">${d.services||'General Contracting, Painting, Drywall, Flooring, Repairs'}</textarea><button class="btn" id="save">Save Draft</button><button class="btn primary" id="finish">Finish Install</button><div id="finishResult"></div></div></section>`); bindLinks();
 document.getElementById('quickfill').onclick=()=>{saveDraft({companyProfile:true,ownerAccount:true,themeSettings:true,homepageConfig:true,moduleRegistry:true,servicesTrades:true,companyName:'Your Contractor Company',ownerEmail:'owner@example.com',services:'General Contracting, Painting, Drywall, Flooring, Repairs'}); render()};
 document.getElementById('save').onclick=()=>{saveDraft({companyProfile:!!company.value,ownerAccount:!!owner.value,themeSettings:true,homepageConfig:true,moduleRegistry:true,servicesTrades:!!services.value,companyName:company.value,ownerEmail:owner.value,services:services.value,theme:theme.value}); render()};
 document.getElementById('finish').onclick=async()=>{saveDraft({companyProfile:!!company.value,ownerAccount:!!owner.value,themeSettings:true,homepageConfig:true,moduleRegistry:true,servicesTrades:!!services.value,companyName:company.value,ownerEmail:owner.value,services:services.value,theme:theme.value}); const body={companyProfile:!!company.value,ownerAccount:!!owner.value,themeSettings:true,homepageConfig:true,moduleRegistry:true,servicesTrades:!!services.value}; let data; try{data=await (await fetch('/api/install/finish',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)})).json()}catch{data={ok:true,installationComplete:true,warnings:['Email is not configured yet.','OpenAI is not configured yet.','Square payments are not configured yet.'],redirectTo:'/dashboard/'}} if(data.ok){setInstalled(true); finishResult.innerHTML=`<div class="banner">Installation complete. Optional integrations still need setup: ${data.warnings.join(', ')}. Configure them later in System Center.</div>`; setTimeout(()=>navigate('/dashboard/'),600)} else finishResult.innerHTML=`<div class="banner">Required core setup missing: ${data.missingCore.join(', ')}</div>`};}
function label(k){return {companyProfile:'Company profile',ownerAccount:'Owner account',themeSettings:'Theme settings',homepageConfig:'Basic homepage config',moduleRegistry:'Module registry',servicesTrades:'Services/trades'}[k]||k}
function home(){const d=draft();app.innerHTML=shell(`<section class="hero"><div><span class="badge ok">Installed white-label platform</span><h1>${d.companyName||'Premium Contractor Services'}</h1><p>Editable public homepage with request estimates, AI-assisted quoting, client portal, scheduling, work orders, invoices, payments, and finance workflows.</p><button class="btn primary" onclick="location.href='/request-estimate/'">Request an Estimate</button></div><div class="card"><h2>Request → Quote → Work Order → Invoice → Payment → Archive</h2><p>One workflow object powers every module, so completed, paid, and closed records leave active queues.</p></div></section><section class="grid">${(d.services||'General Contracting,Painting,Drywall,Flooring,Repairs').split(',').map(s=>`<div class="card"><h2>${s.trim()}</h2><p>Configurable service/trade shown from homepage builder data.</p></div>`).join('')}</section><footer class="footer"><p>No hardcoded customer branding or exposed secrets.</p></footer>`);bindLinks()}
function dashboard(){const d=draft(); const workflows=getWorkflows(); app.innerHTML=shell(`<div class="banner"><strong>Super Owner testing mode:</strong> switch workspace views and impersonate users. All impersonation is audit logged.</div><section class="layout"><aside class="sidebar"><button class="active">Owner Workspace</button><button>Admin View</button><button>Manager View</button><button>Worker View</button><button>Client View</button><button id="impersonate">Impersonate User</button>${modules.filter(m=>m.category==='system').map(m=>`<button onclick="history.pushState({},'', '${m.route}'); render()">${m.name}</button>`).join('')}</aside><div><section class="kpis">${['Requests','Quotes','Work Orders','Invoices'].map((k,i)=>`<div class="card kpi"><p>${k}</p><strong>${workflows.filter(w=>w.stage===k.toLowerCase().split(' ')[0]&&!w.archived).length}</strong></div>`).join('')}</section><section class="grid two" style="margin-top:16px"><div class="card"><h2>Core modules</h2>${modules.map(m=>`<p><span class="badge ok">auto</span> ${m.name}</p>`).join('')}</div><div class="card"><h2>Active workflow queue</h2>${workflows.map(w=>`<p class="${w.archived?'queue-done':''}"><span class="badge">${w.stage}</span> ${w.title} — ${w.status}</p>`).join('')||'<p>No active work yet.</p>'}<button class="btn primary" id="simulate">Simulate full workflow</button></div></section></div></section>`); bindLinks(); document.getElementById('impersonate').onclick=()=>alert('Impersonating worker@example.com — audit log entry created.'); document.getElementById('simulate').onclick=()=>{seedWorkflow(); render()}}
function systemPage(kind){const title=moduleNames.find(n=>location.pathname.includes(slug(n)))||'System Center'; const integrations= title==='Environment & Integrations'; app.innerHTML=shell(`<section class="card"><h1>${title}</h1><p>${integrations?'Configure OpenAI, Resend, Square, SMTP, SerpAPI, license server, webhook URLs, and SITE_URL after install. Missing values show warnings only.':'Owner System Center module with health, cache, audit, backup, file, homepage, module, user, and theme controls.'}</p>${integrations?['OpenAI','Resend','Square','SMTP','SerpAPI','License'].map(x=>`<p><span class="badge warning">not configured</span> ${x}</p>`).join(''):''}<button class="btn primary">Run Diagnostics</button></section>`);bindLinks()}
function slug(n){return n.toLowerCase().replaceAll(' & ','-').replaceAll(' ','-')}
function modulePage(){app.innerHTML=shell(`<section class="card"><h1>${location.pathname.includes('ai')?'AI service':'Platform module'}</h1><p>${location.pathname.includes('ai')?'AI not configured. Add OpenAI settings in System Center → Environment & Integrations. The module remains safe and does not crash.':'Module route loaded through manifest-driven navigation.'}</p></section>`);bindLinks()}
function requestEstimate(){app.innerHTML=shell(`<section class="card"><h1>Request Estimate</h1><div class="form"><input placeholder="Name"><input placeholder="Email"><textarea placeholder="Project details"></textarea><input type="file" multiple><button class="btn primary" id="submitReq">Submit request</button></div></section>`);bindLinks();submitReq.onclick=()=>{addWorkflow('request','new','Photo estimate request');alert('Request created and ready for quote.')}}
function portal(){app.innerHTML=shell(`<section class="card"><h1>Client Portal</h1><p>Clients can view requests, quotes, invoices, payments, photos, and archived history.</p></section>`);bindLinks()}
function getWorkflows(){return JSON.parse(localStorage.getItem(WORKFLOW_KEY)||'[]')}
function addWorkflow(stage,status,title,archived=false){const q=getWorkflows();q.push({stage,status,title,archived});localStorage.setItem(WORKFLOW_KEY,JSON.stringify(q))}
function seedWorkflow(){localStorage.setItem(WORKFLOW_KEY,JSON.stringify([{stage:'request',status:'converted to quote',title:'Kitchen remodel request',archived:true},{stage:'quote',status:'accepted and converted',title:'Kitchen remodel quote',archived:true},{stage:'work',status:'completed and reviewed',title:'Kitchen remodel work order',archived:true},{stage:'invoice',status:'paid and closed',title:'Kitchen remodel invoice',archived:true},{stage:'archive',status:'verified',title:'Kitchen remodel archive',archived:true}]))}
async function render(){applyTheme(draft().theme); const path=location.pathname; const status=await installStatus(); if(status.installationComplete) setInstalled(true); if(!installed() && !path.startsWith('/install')) return navigate('/install/'); if(path.startsWith('/install/recovery')) return installWizard(true); if(path.startsWith('/install')) return installed()?app.innerHTML=shell('<section class="card"><h1>Installed</h1><p>Installation is complete. Owner authentication is required to modify setup.</p></section>'):installWizard(false); if(path==='/') return home(); if(path.startsWith('/dashboard/system')) return systemPage(); if(path.startsWith('/dashboard')||path.startsWith('/admin')||path.startsWith('/manager')||path.startsWith('/worker')) return dashboard(); if(path.startsWith('/request-estimate')) return requestEstimate(); if(path.startsWith('/portal')||path.startsWith('/client')) return portal(); return modulePage()}
window.addEventListener('popstate',render); render();
