import { json, writeState, optionalWarnings, discoverModules } from './_lib/store.js';
const required=['companyProfile','ownerAccount','themeSettings','homepageConfig','moduleRegistry','servicesTrades'];
export async function handler(event){try{const body=event.body?JSON.parse(event.body):{}; const missing=required.filter(k=>!body[k]); if(missing.length) return json({ok:false,installationComplete:false,missingCore:missing,message:'Required core setup is incomplete.'},400); const state=writeState({installation_complete:true,installed_at:new Date().toISOString(),installed_version:'1.0.0',current_step:'complete',bootstrap_generated:true,metadata:{modules:discoverModules().map(m=>m.id)}}); return json({ok:true,installationComplete:true,installed:true,installedAt:state.installed_at,installedVersion:'1.0.0',warnings:optionalWarnings(),redirectTo:'/dashboard/',modulesRegistered:discoverModules().length});}catch(e){return json({ok:false,installationComplete:false,message:'Finish install failed safely.',error:e.message},500)}}
