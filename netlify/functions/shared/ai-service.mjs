export async function runAI({mode='quote',model=process.env.OPENAI_MODEL||'gpt-5.5',input={},images=[]}){if(!process.env.OPENAI_API_KEY){return {ok:false,code:'AI_NOT_CONFIGURED',message:'OpenAI is not configured; request saved for manual review.',mode,model};} return {ok:true,mode,model,data:{summary:'AI service contract ready',lineItems:[{kind:'labor',description:'Staff-reviewed labor estimate',quantity:1,unitPrice:0,total:0}],images:images.length},usage:{estimated:true}};}
