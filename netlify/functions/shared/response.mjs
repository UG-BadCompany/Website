export function json(statusCode, body, headers={}) { return { statusCode, headers: { 'content-type':'application/json; charset=utf-8', 'cache-control':'no-store', ...headers }, body: JSON.stringify(body) }; }
export async function readJson(event) { try { return event.body ? JSON.parse(event.body) : {}; } catch { return {}; } }
export function publicEnvStatus() { const names=['SITE_URL','MAGIC_LINK_FROM_EMAIL','RESEND_API_KEY','OPENAI_API_KEY','SQUARE_ACCESS_TOKEN','SQUARE_LOCATION_ID','SESSION_SECRET','DATABASE_URL']; return Object.fromEntries(names.map(name => [name, { configured: Boolean(process.env[name]) }])); }
