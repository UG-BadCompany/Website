const transitions={ 'request.created':'request_new','quote.drafted':'quote_draft','quote.sent':'quote_sent','quote.accepted':'quote_accepted','quote.converted':'work_order_created','workorder.created':'waiting_assignment','workorder.assigned':'assigned','workorder.scheduled':'scheduled','workorder.started':'in_progress','workorder.completed':'worker_completed','admin.reviewed':'invoice_ready','invoice.sent':'invoice_sent','payment.received':'paid','payment.verified':'payment_verified','archive':'archived','cancel':'cancelled' };
export async function transition({entityType,entityId,event,userId,metadata={}}){ if(!entityType||!entityId||!event) return {ok:false,code:'VALIDATION_ERROR',message:'entityType, entityId, and event are required.',missing:['entityType','entityId','event'].filter(k=>!({entityType,entityId,event})[k])}; const status=transitions[event]; if(!status) return {ok:false,code:'INVALID_WORKFLOW_EVENT',message:`Unsupported workflow event: ${event}`}; const terminal=['paid','payment_verified','closed','archived','cancelled','completed'].includes(status); return {ok:true,data:{entityType,entityId,event,status,active:!terminal,userId,metadata,createdAt:new Date().toISOString()},message:'Workflow moved forward.'}; }
