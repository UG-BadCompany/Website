import { json } from './shared/json.mjs'; import { moduleApiRegistry } from '../generated/module-api-registry.mjs';
export async function handler(event){const parts=(event.path||'').split('/api/modules/')[1]?.split('/').filter(Boolean)||[]; const moduleId=parts.shift(); if(!moduleId) return json(400,{ok:false,code:'MODULE_REQUIRED',message:'Module id is required.'}); const reg=moduleApiRegistry.routes.find(r=>r.moduleId===moduleId); if(!reg) return json(404,{ok:false,code:'MODULE_NOT_FOUND',message:'Module is not registered.'}); if(!reg.routes.some(r=>(r.method||[]).includes(event.httpMethod))) return json(405,{ok:false,code:'METHOD_NOT_ALLOWED',message:'This module action does not allow that method.'}); return json(200,{ok:true,data:{moduleId,path:'/'+parts.join('/'),records:[]},message:'Module API dispatcher routed the request.'});}
